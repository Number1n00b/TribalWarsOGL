#include <stdio>
#include <string>

using std::string;


//A class that I can use that manages all error checkin internally.
class FileReader{

    public:
        FileReader(string filePath);


    private:


}
